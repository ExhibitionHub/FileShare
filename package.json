{
  "name": "exhibitionhub-fileshare",
  "version": "1.0.0",
  "private": true,
  "description": "API simple de publication d'images et de partage par QR code.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/config.js && node --check src/storage.js && node --check src/app.js && node --check src/server.js && npm test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
